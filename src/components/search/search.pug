form.search(action='#')
  input.search__field(
    name='main_search'
    minlength='2'
    autocomplete='off'
    required
    placeholder='Поиск по сайту'
    data-msg-container='.search__error'
  )
  .search__error.search__error_mobile
  .search__actions
    .search__select
      select
        option(selected) Все категории
        option Категория 1
        option Категория 2
        option Какая-то длинная категория
    button.search__submit Поиск
.search__error.search__error_desktop
